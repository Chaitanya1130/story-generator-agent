# Core App Requirements
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.4.2
python-dotenv==1.0.0
requests==2.31.0

# ML/Core Utils (lighter version)
numpy==1.24.3
pillow==10.1.0

# Optional: Use sentence-transformers lite (or remove for now)
sentence-transformers==2.2.2

# Optional: Use qdrant-client only if necessary
qdrant-client==1.6.0

# Optional: Remove if not calling OpenAI yet
openai==1.3.0
